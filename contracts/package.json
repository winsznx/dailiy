{
    "name": "dailiy-contracts",
    "version": "1.0.0",
    "description": "Smart contracts for Dailiy",
    "scripts": {
        "test": "npx hardhat test",
        "compile": "npx hardhat compile",
        "deploy": "npx hardhat run script/deploy.ts"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^5.0.0",
        "hardhat": "^2.22.0",
        "typescript": "^5.0.0",
        "ts-node": "^10.0.0"
    },
    "dependencies": {
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.0.0"
    }
}